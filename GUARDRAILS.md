# 🛡️ Genesis AI Studio 가드레일

프로젝트의 품질과 일관성을 유지하기 위한 핵심 규칙입니다.

---

## ✅ 핵심 규칙 체크리스트 (Top 10)

### 언어 및 문서화
- [ ] 모든 주석은 **한글**로 작성했는가?
- [ ] 작업 계획 및 Task는 **한국어**로 작성했는가?

### 코드 품질
- [ ] 함수는 **20줄 이하**이고 **단일 책임**을 가지는가?
- [ ] **매직 넘버** 없이 상수를 사용했는가?
- [ ] **가드 절**로 중첩 if/else를 최소화했는가?

### 변경 관리
- [ ] 코드 수정 후 **즉시 실행 검증**을 했는가?
- [ ] 의존성이 있는 파일을 **함께 업데이트**했는가?
- [ ] **린터**를 통과했는가? (`ruff check . --fix`)

### UI/UX
- [ ] **Neobrutalism 스타일 가이드**를 준수했는가?
- [ ] **로깅**으로 실행 과정을 추적 가능하게 했는가?

---

## ⚠️ 자주 위반되는 패턴 Top 5

| #   | 패턴                                                        | 해결책                          |
| --- | ----------------------------------------------------------- | ------------------------------- |
| 1   | **import 누락** - 새 클래스 사용 시 import 잊음             | 사용 전 import 확인             |
| 2   | **예외 클래스 미정의** - 서비스에서 사용하는 Exception 누락 | `exceptions.py`에 미리 정의     |
| 3   | **상대/절대 경로 혼용** - 패키지 내부에서 절대 경로 사용    | 패키지 내부는 상대 경로 사용    |
| 4   | **테스트 없이 수정** - 수정 후 실행 검증 생략               | 모든 수정 후 `python -c` 테스트 |
| 5   | **대량 리팩터링** - 한 번에 많은 파일 수정                  | 파일 단위로 작게 나눠서 수정    |

---

## 🔄 검증 절차

```bash
# 1. 린트 검사
ruff check . --fix

# 2. 타입 체크 (optional)
mypy .

# 3. 테스트 실행
pytest

# 4. 앱 실행 확인
streamlit run src/genesis_ai/presentation/app.py
```

> **규칙**: 린터와 기본 테스트를 통과하기 전까지 코드는 "완료"가 아닙니다.
